# משימת Claude Code — בדיקה והשלמת מערכת הניקיון לפני הזנת נתונים אמיתיים

פעל כמהנדס תוכנה ראשי, ארכיטקט מערכת, בודק אבטחה ומנהל מוצר טכנולוגי. בצע בדיקה מקיפה של כל פרויקט מערכת הניקיון הקיים, זהה מה חסר, מה דורש שיפור, מה כבר בנוי היטב ומה לא נכון לפתח.

## המטרה

אני רוצה לסיים עכשיו ככל האפשר את כל הקוד, התשתיות, המסכים, מנועי החישוב, ההרשאות, הבדיקות ואפשרויות הייבוא. את הנתונים העירוניים האמיתיים אמלא רק לאחר שהמערכת תהיה מוכנה.

אין להמציא נתונים אמיתיים, אין לבצע שיוכים לא מבוססים ואין להציג תחזיות, זמני נסיעה או חיסכון כספי כאילו הם מבוססים על מידע אמיתי.

## כללי עבודה

1. קרא את כל הפרויקט לפני שינוי: `CLAUDE.md`, `AGENTS.md`, `README`, `package.json`, קובצי lock, `.env.example`, Prisma schema, migrations, seed, `docs`, tests, API routes, server actions ומסכים.
2. אם קיימת `./claudephotos/`, התייחס למסמכים שבה כמקורות עזר בלבד.
3. אל תניח שפיצ'ר קיים בגלל שהוא מופיע במסמך — ודא שהוא ממומש בקוד, במסד, בממשק ובבדיקות.
4. אל תבנה מחדש דברים שעובדים ואל תשכתב את הפרויקט ללא צורך.
5. שמור על שינויים קיימים ואל תמחק קוד ללא הצדקה.
6. בדוק את גרסאות הטכנולוגיות בפועל ואל תשדרג ספריות מרכזיות ללא סיבה.
7. נתוני הדגמה מותרים רק במצב `DEMO_MODE`, עם סימון בולט והפרדה מלאה מ־production.
8. אם חסר API או מידע אמיתי, בנה interface, adapter, validation ומסך הגדרה, אך אל תמציא תוצאה.
9. אל תעצור בדוח: לאחר הבדיקה יישם את כל משימות P0 ו־P1 שאפשר לבצע ללא נתונים אמיתיים.
10. הוסף או עדכן בדיקות בכל שינוי משמעותי והריץ אותן אחרי כל שלב.

## עובדות עסקיות שחייבות להישמר

- קיימים שני אזורי מכרז חוזיים ועשרה אזורים תפעוליים עירוניים. אלו ישויות שונות.
- אין לנחש לאיזה אזור מכרז שייך כל אזור תפעולי. עד להזנה ואישור, הסטטוס יהיה `REQUIRES_REVIEW`.
- רחוב יכול לחצות כמה אזורים, ולכן יש לעבוד גם עם `StreetSegment` ו־`PathSegment`.
- קיימת חברה חיצונית שמספקת מצלמות ונתוני מיקום לרכבים.
- אין לפתח GPS חדש, מעקב חי, מצלמות, שמירת וידאו, ניתוח וידאו או נתוני מיקום מזויפים.
- אפשר להכין אינטגרציה עתידית לקריאה בלבד, כבויה עד לקבלת API רשמי ואישור.

הנוסח הנכון בתיעוד ובמערכת:

> מצלמות ונתוני מיקום מסופקים באמצעות מערכת חיצונית קיימת ואינם חלק מפיתוח מערכת הניקיון. המערכת מתמקדת בתכנון, הקצאת משימות, ניהול ביצוע, דיווח תפעולי ובקרת איכות. תישמר אפשרות עתידית לאינטגרציה לקריאה בלבד, בכפוף לקבלת API ואישור מהגורמים המוסמכים.

## שלב 1 — Audit ותוכנית

מפה את:

- מבנה הפרויקט והנתיבים.
- מודלי הנתונים והקשרים.
- migrations ו־seed.
- Authentication, RBAC והרשאות לפי אזור.
- מפה ו־PostGIS.
- מנוע עדיפות, תכנון, הקצאה ומסלולים.
- מים, פריקת פסולת ומגבלות רכב.
- תלונות, ליקויים וביקורות.
- `/my-day` ועבודת שטח.
- תמונות וקבצים.
- דוחות ו־Audit Log.
- tests, deployment, monitoring וגיבויים.

צור ועדכן:

- `docs/PRODUCTION_READINESS_AUDIT.md`
- `docs/PRE_DATA_COMPLETION_PLAN.md`
- `docs/REAL_DATA_ONBOARDING_CHECKLIST.md`
- `docs/EXTERNAL_INTEGRATIONS.md`
- `IMPLEMENTATION_STATUS.md`

לכל ממצא כתוב: מזהה, חומרה, מצב קיים, מה חסר, קבצים רלוונטיים, סיכון, פתרון, האם ניתן לבצע עכשיו, האם נדרשים נתונים אמיתיים וסטטוס ביצוע.

חלק לארבע קבוצות: לסיים עכשיו; להכין עכשיו ולהפעיל אחרי נתונים; דורש שירות חיצוני; לא צריך לפתח.

## שלב 2 — מודל נתונים

בדוק והוסף רק אם חסר:

- `Organization`
- `ContractArea`
- `OperationalZone`
- `ZoneAssignment`
- `Street`, `StreetSegment`, `PathSegment`
- `WaterRefillPoint`, `WasteDisposalPoint`
- `Vehicle`, `VehicleOperationalProfile`
- `Employee`, `Shift`
- `CleaningTask`, `RoutePlan`, `RouteStop`, `TaskExecution`
- `FieldObservation`, `Complaint`, `Defect`, `Inspection`
- `SourceDocument`, `SourceEvidence`
- `ManualOverride`, `AuditLog`, `ExternalSystemReference`

לפי הצורך שמור: סטטוס, מקור, רמת ביטחון, גרסה, תוקף, יוצר, מעדכן, timestamps, מחיקה לוגית והערת אישור. אין לקודד עיר, רחוב, קבלן, רכב או תדירות בתוך הלוגיקה.

## שלב 3 — מרכז קליטת נתונים

בנה או השלם ייבוא Excel, CSV, GeoJSON והזנה ידנית עבור אזורים, רחובות, מקטעים, שבילים, נקודות מים, נקודות פריקה, רכבים, עובדים, משמרות, תדירויות ודירוגי לכלוך.

הזרימה: העלאה, התאמת עמודות, preview, validation, כפילויות, שגיאות, אזהרות, אישור, transaction, דוח תוצאות, rollback ו־Audit Log. OCR לעולם לא נכנס אוטומטית ללא אישור מנהל.

## שלב 4 — מפה וגיאומטריה

בדוק Polygon/MultiPolygon/LineString, `ST_Intersection`, `ST_Within`, `ST_Intersects`, תיקון גיאומטריה, פיצול רחוב שחוצה אזורים, מניעת כפילויות, אינדקסים מרחביים, עריכת גבולות, היסטוריית גרסאות, Undo ואזורים לא משויכים.

מסד ריק חייב להציג onboarding ו־empty state ולא מפה שנראית כאילו היא מלאה.

## שלב 5 — עדיפות ולמידה

בדוק והשלם `DirtPriorityScore` שקוף בטווח 0–100 על בסיס גורמים configurable: לכלוך בסיסי, זמן מהניקיון האחרון, תדירות, תלונות, ליקויים, מוקדי קהל, יום, עונה, ביצועים קודמים ודיווח מנהל.

כל ציון יציג הסבר, משקלים, גרסת נוסחה ורמת ביטחון. מנהל יכול לבצע override מתועד.

סטטוסים:

1. `REQUIRES_REVIEW`
2. `MANUAL_BASELINE`
3. `RULE_BASED`
4. `DATA_INFORMED`

מספר הביצועים למעבר ל־`DATA_INFORMED` יהיה configurable, ברירת מחדל 10. הכן EWMA שקוף לזמן ניקיון, צריכת מים ורמת לכלוך, אך אל תציג אותו כ־AI שכבר למד.

## שלב 6 — מים, פסולת ורכבים

לרכב ניתן להגדיר קיבולת מים ופסולת, צריכה, מהירות עבודה, רוחב מעבר, סוגי משימות, זמן מילוי ופריקה, מגבלות גישה ושעות פעילות.

לנקודת שירות ניתן להגדיר מיקום, סוג, שעות, זמן שירות, התאמת רכב, סטטוס, מקור ותאריך בדיקה.

המנוע חייב לזהות מחסור במים, צורך במילוי או פריקה, רכב לא מתאים, נקודה סגורה, חריגה ממשמרת ותוכנית בלתי אפשרית. תוכנית כזאת לא תפורסם ללא אזהרה ו־override מתועד.

## שלב 7 — מסלולים

בדוק את `Nearest Neighbor` ו־`2-opt`, והפרד בין בחירת משימות, סדר עצירות, מטריצת זמני נסיעה, מגבלות, מים/פריקה ובדיקת משמרת.

צור `RoutingProvider` מתחלף. ללא API כבישים אמיתי, חישוב אווירי מותר רק ב־simulation ומסומן `ESTIMATED`; אסור להציגו כזמן כביש אמיתי ואסור mock ב־production.

הכן שלוש חלופות: מהירה, חסכונית ובעלת עדיפות ניקיון. לכל חלופה הצג הסבר, זמן, מרחק, עצירות ומשימות שלא נכנסו.

## שלב 8 — תכנון והקצאה

השלם workflow: טיוטה, תאריך, משמרת, אזור, עובדים, רכבים, משימות, חלופות, feasibility, אזהרות, אישור, פרסום, שינוי גרסאי, ביטול, סיום והשוואת תכנון מול ביצוע.

מנע הקצאה כפולה של עובד או רכב באותו זמן.

## שלב 9 — עובד שטח

בדוק והשלים `/my-day`: משימות, סדר, פרטים, זמן, התחלה/סיום, לכלוך לפני ואחרי, תמונות, מחסור במים, נקודה לא פעילה, חסימה, תקלה, הערה, סיבת אי־ביצוע, חתימת סיום, offline queue, סנכרון, idempotency והתנגשויות. אין צורך ב־GPS פנימי.

## שלב 10 — אבטחה וקבצים

אם תמונות נשמרות במסד, צור abstraction ל־Object Storage עם signed URLs, הרשאות, מגבלת גודל, MIME validation, metadata, retention ו־fallback מקומי לפיתוח בלבד.

בדוק Authentication, RBAC, הרשאה לפי אזור, API/server actions, schema validation, IDOR, CSRF, rate limiting, uploads, secrets, sessions ו־Audit Log. הוסף בדיקות הרשאה אמיתיות.

## שלב 11 — מוכנות, דוחות ומסחריות

בנה dashboard מוכנות: אזורים משויכים, רחובות חסרים, מקטעים ללא תדירות או לכלוך, רכבים ללא פרופיל, נקודות שירות לא מאומתות, מקורות לבדיקה והתנגשויות.

בדוק דוחות תכנון מול ביצוע, אי־ביצוע, זמן נסיעה/עבודה, מים, פריקה, תלונות, ליקויים, עומס, שימוש במשאבים, איכות נתונים, overrides וחריגות.

אין להציג חיסכון כספי לפני הזנת עלויות אמיתיות. דוח GPS או סטייה ממסלול יהיה `Future External Integration` בלבד.

הוצא hard-coded branding של כפר סבא להגדרות Organization: שם, לוגו, צבעים, אזור זמן, שפה, פורמט תאריך, Feature Flags והגדרות נוסחה. אל תבצע שכתוב מסוכן רק לצורך multi-tenancy.

## שלב 12 — בדיקות ו־Production

הרץ ותקן: TypeScript, lint, build, unit, integration, Playwright, Prisma validation, migrations על DB חדש, RTL, נגישות, הרשאות, מסלולים, מים, חפיפות, rollback, empty state ו־offline sync.

בדוק גם `.env.example`, environment validation, health endpoint, structured logging, monitoring adapter, backups, indexes, pooling, transactions, pagination, N+1, error boundaries, idempotency, staging/production ו־rollback plan.

## לא לפתח עכשיו

- GPS או מצלמות חדשים.
- וידאו או זיהוי לכלוך בווידאו.
- מעקב עובדים נסתר.
- אינטגרציה לא רשמית.
- Machine Learning כבד ללא נתונים.
- תחזיות או חיסכון מומצאים.
- שיוך אזורים, רחובות או נקודות מים ללא מקור.
- אפליקציות native נפרדות אם PWA מספיקה.
- שכתוב מלא רק בגלל העדפה טכנולוגית.

## סדר עדיפות

### P0

Build, TypeScript, migrations, אבטחה, הרשאות, אובדן מידע, hard-coded data, ערבוב סוגי אזורים, מידע מזויף, פרסום תוכנית בלתי אפשרית, קבצים לא מאובטחים ו־Audit חסר.

### P1

ייבוא, onboarding, מודל גיאוגרפי, עדיפות, מים/פריקה, routing abstraction, תכנון, `/my-day`, offline, דוחות ו־production readiness.

### P2

UX, סימולטור, מיתוג, דוחות מתקדמים ואופטימיזציה לא קריטית.

### P3

אינטגרציות עתידיות ו־ML מתקדם.

## אופן הביצוע

1. בצע audit ועדכן את מסמכי התכנון.
2. הצג תקציר קצר ואז התחל מיד ב־P0 וב־P1.
3. הרץ בדיקות אחרי כל שלב.
4. עדכן `IMPLEMENTATION_STATUS.md` בפועל; אל תסמן משימה שהופיעה רק בתיעוד.
5. אם ה־context מתקרב לסיום, כתוב checkpoint: מה הושלם, מה נשאר, בדיקות והפקודה המדויקת להמשך.
6. אל תשנה בדיקה רק כדי שתעבור אם ההתנהגות העסקית שגויה.

## תנאי קבלה

- Build, migrations והבדיקות הקריטיות עוברים.
- DB ריק מציג onboarding ולא נשבר.
- ניתן לייבא ולתקן נתונים.
- אזורים יכולים להישאר לא משויכים.
- אין שיוכים, GPS, מצלמות, תחזיות או עלויות מזויפים.
- תוכנית לא אפשרית אינה מתפרסמת ללא override.
- demo מופרד מ־production.
- קיימים RBAC, אחסון מאובטח ו־Audit Log.
- חישובים מציגים מקור, ביטחון והסבר.
- קיים checklist מדויק לנתונים האמיתיים שאזין בהמשך.

## הדוח הסופי

החזר:

1. ציון מצב לפני השינויים.
2. מה היה בנוי טוב.
3. כל הבעיות שנמצאו.
4. מה תוקן בפועל.
5. קבצים ו־migrations ששונו.
6. בדיקות שנוספו ותוצאות כל הפקודות.
7. מה דורש נתונים אמיתיים.
8. מה דורש API חיצוני.
9. מה לא נכון לפתח.
10. סיכונים לפני פיילוט.
11. רשימת הנתונים שצריך לקבל מהעירייה או ממנהל העבודה.
12. עשרת השיפורים הבאים בעלי הערך הגבוה ביותר.
13. ציוני מוכנות חדשים ל־Demo, Pilot, Production ו־Commercial Product.

המטרה היא קוד יציב, מאובטח, ניתן לבדיקה ומוכן להזנת נתונים אמיתיים — ללא המצאת מידע וללא בנייה מחדש של מערכות GPS ומצלמות קיימות.
